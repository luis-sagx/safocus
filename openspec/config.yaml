# openspec/config.yaml — SaFocus
# Auto-generated by sdd-init on 2026-06-06
schema: spec-driven

context: |
  Project: safocus — Anti-Procrastination Flutter app (Android) + Chrome extension (MV3)
  SDK: Flutter 3.7+, Dart 3.11.5
  State: flutter_riverpod + riverpod_generator (codegen, .g.dart in .dart_tool/)
  Navigation: go_router
  Storage: SharedPreferences (settings), flutter_secure_storage (PIN hash), LocalStorage (JSON)
  Auth: local_auth (biometrics) + PIN (SHA-256 via crypto pkg)
  Notifications: flutter_local_notifications
  Background: workmanager
  DNS blocking: Android VPN TUN interface
  Localization: es/en, inline strings (no ARB), set via SettingsProvider
  Architecture: Feature-first — lib/{core, data, navigation, features/}
  Chrome extension: chrome_extension/ standalone JS/HTML, declarativeNetRequest, chrome.storage
  CI: none detected

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (feature-first, riverpod providers)
    - Run `dart run build_runner build --delete-conflicting-outputs` after riverpod changes
    tdd: false
    test_command: "flutter test"
  verify:
    test_command: "flutter test"
    build_command: "flutter build apk --debug"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  reason: "Test runner exists (flutter test) but 0 real tests — only placeholder 'expect(1+1,2)'. Strict TDD would block ALL changes to existing untested code. Start with test-then-code for new features, not retroactive."
  runner:
    command: flutter test
    framework: flutter_test (package:test)
  layers:
    unit: { available: true, tool: "flutter_test" }
    integration: { available: false, tool: "—" }
    e2e: { available: false, tool: "—" }
  coverage:
    available: false
    command: "—"
  quality:
    linter: { available: true, command: "flutter analyze" }
    type_checker: { available: true, command: "dart analyze" }
    formatter: { available: true, command: "dart format" }

current_state:
  analyze: "31 issues — 3 warnings (unused imports, unused local var), 28 infos (deprecated withOpacity/activeColor). No errors."
  tests: "1 placeholder test passes. 0 real tests. No coverage."
